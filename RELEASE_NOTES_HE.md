# גרסה 1.8.0

- מעבר ל-R2 `object_key` כמקור קנוני ו-relative `/api/media/...` URLs.
- Migration 0010 לתיקון כתובות מדיה ישנות ולהוספת נתוני נגישות.
- בדיקת R2 Integrity מתוך ממשק הניהול.
- שדות נגישות לכל סדנה עם ברירת מחדל `UNKNOWN`.
- הצהרת נגישות לפי הנוסח שסופק, ללא טענת בדיקה מקצועית או פטור משוער.
- שיפורי מקלדת, Focus Trap, ARIA, ניגודיות ו-Zoom.
- `deploy:dev` / `deploy:prod` במקום deploy עמום.

ראו `docs/RELEASE_NOTES_1_8_HE.md`.

# גרסה 1.6.0

- עיצוב מחדש של מיילי האישור וההתראות.
- כיבוי WhatsApp כברירת מחדל וביטול תור הודעות קיים.
- תיקון רכיב העלאת תמונת סדנה.
- הדגשת פרטי הסדנה בכרטיסים ובעמוד הסדנה.
- Hero עם שני שדות כותרת נפרדים ובקרות פונט/גודל.
- אפשרות להשתמש בתמונה מהגלריה כרקע Hero.

# Release 1.5.0 — Gallery & Status Alignment — 17.07.2026

## ממשק וסטטוסים

- כל תגי הסטטוס ממורכזים כעת אנכית ואופקית בתוך הרקע שלהם.
- נוסף גובה עקבי, `line-height` קבוע ויישור אמצע בכרטיסים ובטבלאות.
- סטטוסים נפוצים בממשק הניהול מוצגים בעברית במקום ערכי מערכת גולמיים.

## גלריה

- נוסף דף גלריה ציבורי לתמונות ולסרטונים.
- נוספה העלאה מתוך ממשק הניהול ל־Cloudflare R2.
- נתמכים JPEG, PNG, WebP, GIF, AVIF, MP4 ו־WebM.
- נוספו כותרת, כיתוב, תיאור נגיש, סדר תצוגה וסטטוס פרסום.
- ניתן לערוך, להסתיר ולמחוק תוכן לצמיתות.
- תמונות נפתחות ב־Lightbox וסרטונים ניתנים לניגון ישירות.
- נתיב המדיה תומך ב־HTTP Range Requests לניגון יעיל ולדילוג בתוך סרטון.
- נוספה תצוגה מקדימה של הגלריה בדף הבית לשני העיצובים.

## מסד ובדיקות

- נוספה מיגרציה `0007_gallery_library.sql`.
- 37 טבלאות PostgreSQL נטענו בהצלחה.
- נבדקו פרסום גלריה ומחיקה ב־Cascade.
- `npm run validate`, `wrangler deploy --dry-run` ו־`npm audit --omit=dev --audit-level=high` עברו.

---

# Release 1.4.0 — Classic Color Palettes — 17.07.2026

## עיצוב

- נוספו חמש פלטות צבעים מוכנות לעיצוב `Classic`: Rosin, Plum, Ocean, Sage ו־Midnight.
- הפלטה משנה את מערכת הצבעים המלאה של האתר הציבורי, כולל רקעים, טקסטים, אזורי במה, כפתורים, קישורים, Hero ותפריט המובייל.
- נוספו כרטיסי בחירה עם דוגמיות צבע וקישורי Preview לכל פלטה.
- הבחירה נשמרת במסד ומוחלת מיד ללא Deploy נוסף.
- עיצוב `Modern`, ממשק הניהול והלוגיקה העסקית אינם משתנים.

## מסד ובדיקות

- נוספה מיגרציה `0006_classic_color_palettes.sql` עם ברירת מחדל `ROSIN` ו־Constraint לערכים מותרים.
- `npm run validate` עבר עם כל חמש הפלטות ועם בדיקת דחיית ערך לא חוקי.
- `wrangler deploy --dry-run` עבר.
- `npm audit --omit=dev --audit-level=high` החזיר 0 חולשות.

---

# Release 1.3.0 — Dual Theme & Portal Mobile Drawer — 17.07.2026

## עיצוב

- נוספה בחירה בין `Classic` לבין `Modern` מתוך הגדרות המנהל.
- `Classic` משחזר את העיצוב הקודם, כולל Hero גרפי, טיפול תמונות קודם ופריסת טפסים קלאסית.
- `Modern` שומר את העיצוב הצילומי והגלריה הנוכחית.
- שני העיצובים משתמשים באותה לוגיקה, באותו Backend ובאותם נתונים.
- נוספו קישורי תצוגה מקדימה שאינם משנים את העיצוב הפעיל.
- ברירת המחדל לאחר המיגרציה היא `Classic`.

## מובייל

- תפריט האתר הציבורי הועבר ל־React Portal תחת `document.body`.
- התיקון מונע חיתוך והתכווצות שנגרמו על ידי ה־Sticky Header וה־`backdrop-filter` ב־Chrome Mobile.
- נוספו Focus Trap, החזרת Focus, גובה `100dvh`, גלילה פנימית ויעדי מגע מלאים.
- בדיקת CSS ב־Viewport של 390×844 עברה: Drawer בגובה המסך וכל ששת הקישורים גלויים ולחיצים.

## מסד ובדיקות

- נוספה מיגרציה `0005_dual_public_themes.sql`.
- `npm run validate` עבר עם שתי אפשרויות העיצוב.
- `wrangler deploy --dry-run` עבר.
- `npm audit --omit=dev` החזיר 0 חולשות.

---

# Release 1.2.0 — P0 Closure & Mobile Drawer Fix — 17.07.2026

## סגירת P0

- נוסף Magic Link מאובטח ו־HttpOnly Session לאזור הלקוח.
- נסגרו עמודי סטטוס באמצעות Tokens חתומים הנשמרים כ־Hash.
- נוסף Cloudflare Rate Limiting לנתיבים ציבוריים ולכניסת מנהלים.
- נוספו Turnstile עם אימות שרת, נעילת Login, OTP ואיפוס סיסמה.
- נוספה אינטגרציית PayMe refund ניתנת להגדרה, כולל החזר מפוצל, Retry, Idempotency ומניעת כפל.
- ביטול הרשמה הפך לתהליך אטומי הכולל החזר זכאות, החזרים, הודעות ו־Waitlist.
- העברה בין סדנאות הפכה לאטומית ומוגנת קיבולת ומחיר.
- ספק חסר אינו מסומן עוד כאילו הודעה נשלחה.
- מסמכים משפטיים נשמרים כטיוטה ודורשים אישור OWNER מפורש.
- נוסף שער מוכנות לייצור שמחייב עסקה והחזר אמיתיים, Turnstile, MFA, דוא״ל, פרטי עסק ומסמכים מאושרים.

## מובייל

- תוקן Drawer ציבורי שהתכווץ ב־Chrome Mobile.
- תוקן גם Drawer הניהול.
- הוגדרו רוחב viewport קשיח, `flex-shrink: 0`, גלילה פנימית וכפתורים ברוחב מלא.
- במסכים עד 380px התפריט נפתח ברוחב מלא.

## מסד ובדיקות

- נוספה מיגרציה `0004_p0_security_and_operations.sql`.
- 36 טבלאות נטענו בהצלחה.
- `npm run validate` עבר במלואו.
- `wrangler deploy --dry-run` עבר עם R2 ושני Rate Limit bindings.
- `npm audit` האחרון לא החזיר דוח בגלל HTTP 502 של Registry; חובה להריץ מחדש לפני ייצור.

## אבטחת סודות

- הוסרה מחרוזת Neon שנראתה אמיתית מקובץ `.env.example`.
- כאשר החיבור היה פעיל, חובה לסובב את הסיסמה ב־Neon.

---

# Release 1.1.1 — UI Audit & Workshop Filters — 17.07.2026

## תיקונים

- אוחד ה־Frontend המודרני לתיקיית ה־Workspace שבאמת נבנית בפריסה.
- הוסרה תיקיית Frontend כפולה שהייתה עלולה לגרום לפריסת UI ישן.
- תוקן יחס הרוחב בין חיפוש הסדנאות לבורר הרמה ב־Chrome.
- נוספה רשימת רמות קבועה, כולל שמירה על רמות מותאמות קיימות.
- שדה הרמה בממשק הניהול הוחלף לבורר אמיתי.
- תוקן כפתור "פתיחה" של קוד סדנה כך שהטקסט אינו נחתך.
- נוסף מצב ריק נגיש כאשר הסינון אינו מחזיר תוצאות.

## ביקורת מוכנות

- נוסף `docs/archive/DEVELOPMENT_GAP_AUDIT_HE.md` עם פערי P0/P1/P2 לפני הפעלה מסחרית.
- `docs/FEATURE_MATRIX.md` עודכן מסטטוס שיווקי לביקורת המבוססת על הקוד בפועל.
- Build, TypeScript, מיגרציות ובדיקות המסד עברו לאחר השינויים.
- בדיקת `npm audit --omit=dev` מוקדמת בסקירה החזירה 0 חולשות ידועות; ניסיון האימות הסופי נחסם ב־502 של שרת ה־Registry ולא החזיר דוח חדש.

---

# Release 1.1.0 — PayMe + Mobile Accessibility — 16.07.2026

## שינויים עיקריים

- נוספה אינטגרציית PayMe מלאה ליצירת Hosted Payment Page בצד השרת.
- נוספה תמיכה ב-`seller_payme_id`, `payme_client_key`, סביבת Sandbox וכתובת API ניתנת להגדרה.
- נוספה כתובת Callback ייעודית עם Secret אקראי.
- Callback מאמת מזהה תשלום, מזהה מוכר, מזהה עסקת PayMe וסכום לפני אישור תשלום.
- Callback כפול נשאר אידמפוטנטי באמצעות פונקציית התשלום האטומית במסד.
- הודעות שגיאה פנימיות של ספק הסליקה אינן נחשפות למשתמש הציבורי.
- PayMe נוסף לצד Mock ו-Tranzila, בלי לשבור את מצב הבדיקות הקיים.

## מובייל ונגישות

- אותו Frontend הוסב ל-mobile-first; לא נוצרה אפליקציה נפרדת שמכפילה לוגיקה ותחזוקה.
- תפריט ציבורי ותפריט ניהול הוסבו ל-Drawer נגיש במסכים קטנים.
- נוסף קישור דילוג לתוכן, Focus ברור וסגירה באמצעות Escape.
- הוגדלו יעדי מגע וכפתורים מרכזיים.
- טופס ההרשמה מוצג במסך מלא בטלפון.
- נוסף כפתור הרשמה קבוע בתחתית עמוד הסדנה.
- נוספה תמיכה ב-Safe Area, Reduced Motion, Increased Contrast וגדלי שדה המתאימים ל-iOS.
- נוספו `autocomplete`, `inputMode`, תוויות ו-ARIA לרכיבים מרכזיים.

## מסמכים חדשים

- `docs/PAYME_SETUP_HE.md`
- `docs/archive/MOBILE_ACCESSIBILITY_HE.md`

## בדיקות שבוצעו

- React production build.
- Cloudflare Worker TypeScript typecheck.
- טעינת 3 מיגרציות ו-32 טבלאות.
- בדיקות קיבולת אטומית, מקדמה, יתרה, כרטיסייה ואידמפוטנטיות.
- בדיקת יצירת עסקת PayMe באמצעות Fetch מדומה ובדיקת ה-Payload וה-`sale_url` המוחזר.
- Cloudflare Wrangler dry-run.
- `npm audit` ללא חולשות ידועות בזמן האריזה.

## מגבלות שחשוב לא להסתיר

- סליקה אמיתית דורשת חשבון PayMe מאושר ומפתחות ייצור.
- כתובת API לייצור צריכה להגיע מ-PayMe; החבילה משתמשת ב-Sandbox כברירת מחדל.
- החזר כספי אוטומטי מול PayMe לא מופעל בלי הרשאות ותיעוד Refund API המתאימים לחשבון.
- הצהרת נגישות סופית דורשת בדיקה ידנית עם התוכן והתמונות האמיתיים.

---

# v1.7.0 — עברית/אנגלית, דוא״ל דו־כיווני ו-SEO Eden Zino

- נוסף בורר עברית / English באתר הציבורי, כולל `lang`/`dir` אוטומטיים ושמירת בחירת המשתמש.
- כל ממשק המשתמש הציבורי תורגם בקוד; תוכן עסקי דינמי תומך בשדות English עם fallback לעברית.
- שפת הלקוח נשמרת בהרשמות, Waitlist והזמנות ומשמשת את תור ההודעות.
- כל הודעות הדוא״ל של הלקוחות שודרגו לתבנית HTML ממותגת עם RTL/LTR בהתאם לשפה.
- OTP ואיפוס סיסמה של Admin הועברו גם הם לתבנית המעוצבת.
- המותג הציבורי שונה ל-`Eden Zino`.
- נוספו favicon SVG/PNG, Apple Touch Icon, `WebSite` structured data, Open Graph, robots.txt ו-sitemap בסיסי.
- הוסר Workflow כפול ומסוכן לפריסת `main` והוסרו קבצי Wrangler היסטוריים.
- מסמכי יישום היסטוריים הועברו ל-`docs/archive/`.

פירוט: `docs/BILINGUAL_EMAIL_SEO_UPGRADE_HE.md` ו-`docs/PROJECT_CLEANUP_HE.md`.
